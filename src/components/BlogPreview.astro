---
/**
 * BlogPreview.astro
 * A főoldalon megjelenő blog előnézet szekció.
 * - hu locale: /blog oldalra mutat, hu cikkeket tölt
 * - en locale: /en/blog oldalra mutat, en cikkeket tölt
 * - ja / ko: nem renderel semmit (nincs elég tartalom)
 *
 * Tipográfia: pontosan illeszkedik a blog.astro fejléc stílusához.
 */
import { getCollection } from 'astro:content';
import type { Locale } from '../i18n/v3/index';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;

// ja/ko: ne renderelj semmit
if (locale === 'ja' || locale === 'ko') return;

const slugOf = (id: string) => id.split('/').slice(1).join('/');

const allPosts = await getCollection('blog', ({ data }) => data.lang === locale);
const posts = allPosts
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 3);

const blogBase = locale === 'hu' ? '/blog' : `/${locale}/blog`;
const postBase = locale === 'hu' ? '/blog' : `/${locale}/blog`;

const labels: Record<string, { eyebrow: string; heading: string; headingEm: string; headingEnd: string; sub: string; cta: string; readmore: string }> = {
  hu: {
    eyebrow: 'Tudástár',
    heading: 'Bőrápolási ',
    headingEm: 'tippek',
    headingEnd: ' és tanácsok',
    sub: 'Szakmai tartalmak a bőröd egészségéért és természetes ragyogásáért.',
    cta: 'Összes cikk',
    readmore: 'Elolvasom',
  },
  en: {
    eyebrow: 'Knowledge',
    heading: 'Skincare ',
    headingEm: 'tips',
    headingEnd: ' and advice',
    sub: 'Professional articles on skincare, treatments and conscious beauty routines.',
    cta: 'All articles',
    readmore: 'Read more',
  },
};

const l = labels[locale] ?? labels['hu'];
---

{posts.length > 0 && (
<section class="bp-section reveal">
  <div class="bp-inner">

    <header class="bp-header">
      <p class="bp-eyebrow">{l.eyebrow}</p>
      <h2 class="bp-title">{l.heading}<em>{l.headingEm}</em>{l.headingEnd}</h2>
      <p class="bp-sub">{l.sub}</p>
    </header>

    <div class="bp-grid">
      {posts.map((post) => (
        <a href={`${postBase}/${slugOf(post.id)}`} class="bp-card">
          <div class="bp-card-body">
            <time class="bp-date" datetime={post.data.pubDate.toISOString()}>
              {post.data.pubDate.toLocaleDateString(
                locale === 'hu' ? 'hu-HU' : 'en-GB',
                { year: 'numeric', month: 'long', day: 'numeric' }
              )}
            </time>
            <h3 class="bp-card-title">{post.data.title}</h3>
            <p class="bp-card-desc">{post.data.description}</p>
          </div>
          <span class="bp-card-cta">
            {l.readmore}
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M5 12h14M12 5l7 7-7 7" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </span>
        </a>
      ))}
    </div>

    <div class="bp-footer">
      <a href={blogBase} class="bp-cta">
        {l.cta}
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
          <path d="M5 12h14M12 5l7 7-7 7" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </a>
    </div>

  </div>
</section>
)}

<style>
  .bp-section {
    background: var(--bg);
    padding: clamp(4rem, 8vw, 6rem) var(--page-margin);
  }

  .bp-inner {
    max-width: 900px;
    margin: 0 auto;
  }

  /* ── Fejléc — 1:1 másolat a blog.astro .blog-header stílusából ── */
  .bp-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .bp-eyebrow {
    font-family: var(--sans);
    font-size: 0.65rem;
    font-weight: 700;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--green);
    margin-bottom: 0.75rem;
  }

  .bp-title {
    font-family: var(--serif);
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 400;
    color: var(--ink);
    line-height: 1.15;
    margin-bottom: 1rem;
  }

  .bp-title em {
    font-style: italic;
    color: var(--green);
  }

  .bp-sub {
    font-family: var(--sans);
    font-size: 1rem;
    color: var(--muted);
    max-width: 50ch;
    margin: 0 auto;
    line-height: 1.6;
  }

  /* ── Kártyarács — 1:1 másolat a blog.astro .blog-grid stílusából ── */
  .bp-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(min(340px, 100%), 1fr));
    gap: 1.5rem;
  }

  .bp-card {
    background: #fff;
    border-radius: 14px;
    border: 1px solid rgba(0,0,0,0.07);
    padding: 1.75rem;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: 1.25rem;
    text-decoration: none;
    color: inherit;
    transition: transform 0.22s cubic-bezier(0.16,1,0.3,1),
                box-shadow 0.22s cubic-bezier(0.16,1,0.3,1);
  }
  .bp-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 12px 32px rgba(80,100,60,0.1);
  }

  .bp-card-body {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }

  .bp-date {
    font-family: var(--sans);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--muted);
  }

  .bp-card-title {
    font-family: var(--serif);
    font-size: 1.25rem;
    font-weight: 500;
    color: var(--ink);
    line-height: 1.3;
  }

  .bp-card-desc {
    font-family: var(--sans);
    font-size: 0.875rem;
    color: var(--muted);
    line-height: 1.6;
  }

  .bp-card-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-family: var(--sans);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--green);
  }

  /* ── "Összes cikk" gomb alul, középen ── */
  .bp-footer {
    text-align: center;
    margin-top: 2.5rem;
  }

  .bp-cta {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    font-family: var(--sans);
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--ink);
    text-decoration: none;
    border-bottom: 1.5px solid var(--green);
    padding-bottom: 2px;
    transition: color 0.18s;
  }
  .bp-cta:hover { color: var(--green); }
</style>
